{
  "name": "@options/lib",
  "version": "0.3.0",
  "main": "index.ts",
  "scripts": {
    "build": "tsup src/index.ts",
    "dev": "yarn run build --watch"
  },
  "devDependencies": {
    "tsup": "^7.1.0",
    "typescript": "^4.9.4"
  }
}
